import { IProps } from "../../types/IProps";

export const H1 = ({ styles, children }: IProps) => {
  return <div style={styles}>{children}</div>;
};

export const H2 = ({ styles, children }: IProps) => {
  return <div style={styles}>{children}</div>;
};

export const H3 = ({ styles, children }: IProps) => {
  return <div style={styles}>{children}</div>;
};

export const H4 = ({ styles, children }: IProps) => {
  return <div style={styles}>{children}</div>;
};
